package seedu.menion.logic;

import seedu.menion.commons.exceptions.IllegalValueException;
import seedu.menion.logic.commands.*;
import seedu.menion.model.activity.ActivityDate;

import static org.junit.Assert.*;

import java.util.ArrayList;

import org.junit.Test;

public class AddCommandTest {

	
	/*@Test
	public void testIsValidDateFromActivityDate() throws IllegalValueException{
		
		assertTrue(ActivityDate.isValidDate("31-08-2016"));
		
		
	}/*
	/*
    @Test
    public void testIsEventObjectCreatedSuccessfully() throws IllegalValueException {
        ArrayList<String> details = new ArrayList<String>();

        details.add("Event"); // type
        details.add("project meeting");
        details.add("celebrate");
        details.add("10-10-2016");
        details.add("1400");
        details.add("10-10-2016");
        details.add("1800");

        AddCommand test = new AddCommand(details);
        
        assertEquals(details.get(0), test.eventStub.type);
        assertEquals(details.get(1), test.eventStub.name);
        assertEquals(details.get(2), test.eventStub.notes);
        assertEquals(details.get(3), test.eventStub.startDate);
        assertEquals(details.get(4), test.eventStub.startTime);
        assertEquals(details.get(5), test.eventStub.endDate);
        assertEquals(details.get(6), test.eventStub.endTime);

    }
    */
    
}
